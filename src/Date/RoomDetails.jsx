// import { useParams } from "react-router-dom";

// const RoomDetails = () => {
//     const { roomId } = useParams(); // Access the room ID from the URL parameter

//   // Find the room details based on the room ID
//   const room = rooms.find((room) => room.id === parseInt(roomId));

//   if (!room) {
//     return <div>Room not found.</div>;
//   }
//   return (
//     <div>RoomDetails</div>
//   )
// }

// export default RoomDetails